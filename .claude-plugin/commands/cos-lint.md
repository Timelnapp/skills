# /cos-lint — Stage 5.5 REVIEW

## Timeln (run first)

**`timeln-warned`** — feed scars into red team.

## Then

1. **`consult-consistency-lint`** → `lint-report.md`
2. **`consult-red-team`** → `red-team-report.md`
3. Run **`/cos-integrity 5.5`** if not done.

**BLOCK** or critical lint → increment `revision_loop` (max 2) → `/cos-design` or `/cos-variants`.

**Checkpoint:** Ship pack?
